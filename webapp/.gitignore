# File generated by the Sass compiler from src/styles/main.scss
src/styles.css

# Temporary folder created by the "js:babel" gulp task.
# The folder is supposed to be cleaned up automatically, but in case it isn't
# (when, for example, the build process is interrupted), we ignore it here.
temp/

# Dependencies
node_modules/
src/bower_modules/
test/bower_modules/

# Build output
dist/

# Cache directory containing the deployment repository
# Created and used by the gulp-gh-pages plugin
.publish/
